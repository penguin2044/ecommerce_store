<div class="container">
  <div class="page-content">
    <% page = Page.find_by(slug: 'about') %>
    <% if page %>
      <h1><%= page.title %></h1>
      <div class="page-text">
        <%= simple_format(page.content) %>
      </div>
    <% else %>
      <h1>About Into the Music</h1>
      <p>Content not found.</p>
    <% end %>
    
    <%= link_to 'Back to Store', root_path, class: 'btn' %>
  </div>
</div>